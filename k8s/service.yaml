apiVersion: v1
kind: Service
metadata:
  name: bootstrap-flask
  labels:
    app: bootstrap-flask
spec:
  type: NodePort
  selector:
    app: bootstrap-flask
  ports:
  - port: 80
    targetPort: 5000
